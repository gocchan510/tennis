# テニス組み合わせアプリ

テニスのダブルス対戦相手を決めるPWA。

## 公開URL

https://gocchan510.github.io/tennis/

## ブランチ運用

- `gh-pages` … 本番（GitHub Pages から配信）
- `claude/setup-git-config-DqQ7z` … 開発ブランチ（変更はここで作ってgh-pagesにも反映）

## 仕様

### プレイヤー管理
- 名前なし、番号で管理（起動時に人数を入力 → 1, 2, 3... と自動採番）
- 人数上限なし
- 途中参加：参加プレイヤーチップ列の末尾の **＋チップ** で追加
- 途中退場：プレイヤーのチップをタップで退場／復帰トグル
- 参加プレイヤーセクションは画面下部に **常時固定表示**（コート／統計どちらのタブでも見える、`view-main` の外に配置）
- **`p.games`（優先度カウンタ）** と **`p.actualGames`（実プレー数）** を分けて管理
  - 表示は常に `actualGames`、スケジューリングは `games`
  - 途中参加・退場からの復帰時は `games` を現アクティブの **最小値** にセット → 「下駄を履かせる」ことで catch-up 暴走を防ぐ

### コート
- 起動時に **1面 / 2面** を選択
- 1面：アクティブプレイヤーが4人以上で表示
- 2面：アクティブプレイヤーが8人以上で表示
- 選択コート数は `state.maxCourts` に保持

### 組み合わせロジック（公平スケジュール）
- 仮想カウンタ（`vg`=出場数, `vpairs`=ペア回数, `vopps`=対戦回数, `vcm`=同コート回数）を実履歴から初期化し、**最大50試合分を一括シミュレート** して順次表示
- 各ステップで全通りを列挙して最適化：
  1. **出場プレイヤー選出**：`vg` の少ない順で必須（required）/ フィラー（filler）に分け、`C(filler, 必要数)` を全列挙
     - 2面の場合は `C(残り, 3)` でコート分割も全パターン評価
     - スコア＝同コート回数の **二乗和**（`vcm`）。同点はランダム選択
  2. **4人内のペア分け**：3通りを全評価
     - スコア＝新ペアの `vpairs` 合計 ＋ 新対戦の `vopps` 合計
     - 同点はランダム選択
- これにより **出場・ペア・対戦の3指標すべてが均等化** される

### 進捗マーカー（記録フロー）
- 組み合わせリストの左ガター（`#courts` 内）に **→ ハンドル** が常駐
- ハンドルをドラッグして消化済みの位置にスナップ → 通過した上の行はグレーアウト
- 指を離した時点で `state.markerPos` の差分だけ `applyCombination` / `revertCombination` を実行
  - 上に戻せば履歴も巻き戻し（`games` `actualGames` `pairs` `opponents` を減算）
- マーカー位置は `localStorage` に保存
- マーカーが下端に近づくと自動で次の `MAX_COMBOS` 試合分を生成
- プレイヤー追加・退場時は **マーカー以下のみ再生成**（消化済みの上は固定）
- 初回（履歴なし & 全員 0 試合）の **1試合目だけ** 決定論的に `1&2 vs 3&4` から開始

### セッション
- 起動するたびにセットアップ画面を表示（人数・コート数を入力）
- スタートで前セッションのデータをリセットして新規作成
- セッション中のデータは `localStorage` に保存
- ヘッダー右の **リセット** ボタン（セッション中のみ表示）で確認ダイアログ → スタート画面に戻す
- スタート画面では前セッションの組み合わせリスト・チップ・「参加中N人」表示は出さない（`renderCourts` / `renderPlayers` が `sessionStarted=false` 時に bail）

### 統計タブ
- プレイヤーごとにカード表示
- 出場回数、ペア履歴（誰と何回）、対戦履歴（誰と何回）を表示

### バージョン表示
- `APP_VERSION`（JST日時）と `VERSION_NOTES`（変更点）をセットアップ画面とフッターに表示
- 更新時は `APP_VERSION` / `VERSION_NOTES` / `sw.js` の `CACHE` 名を同時に書き換える（SWキャッシュ強制更新のため）

## ファイル構成

```
index.html   … 画面構造（メインビュー・統計ビュー・セットアップ画面）
style.css    … モバイルファーストCSS
app.js       … 全ロジック＋レンダリング
manifest.json … PWAマニフェスト
sw.js        … Service Worker（オフライン対応・キャッシュ戦略）
icon.svg     … アプリアイコン（テニスボール風SVG）
```

## 今後の検討事項（未実装）

- セッションをまたいだ累積統計の保持
